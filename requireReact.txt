

<a href={require(`../Ejemplos/${detalle.ejemplo1}`)} target="_blank">Ejemplo1</a>

<div className="card-imagen" style={{backgroundImage: `url(${require(`../images/${detalle.imagen}`)})`}}></div>
<div className="card-imagen" style={{backgroundImage: `url(${(`${configuracion.img.linkImg}${detalle.imagen}`)})`}}></div> //imagen externa url desde json

<img src={require(`../images/${Placetime.PlaceTime.url}`)} alt="" />

src={(`${configuracion.img.linkImg}${detalle.imagen}`)} //imagen externa ruta desde un json

//***************************************************************//
//**Para incluir una imagen tambien se puede hacer un import con un console.log del import para saber la ruta

        <div className="grid-x grid-margin-x" id="series-example">
          {conocimientoData.diplomas.map((diploma, index) => {
            const imagePath = `/src/assets/img/${diploma.imagen}`;
            return (
              <div className="cell small-3" key={index}>
                <a href="" data-toggle="modal" data-target={`#modal-${index}`}>
                  <img className="thumbnail" src={imagePath} id={`series-example-${index + 1}`} />
                </a>
              </div>
            );
          })}
        </div>

//********************************************************************//